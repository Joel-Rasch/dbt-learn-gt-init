sources:
  - name: jaffle_shop #schema name
    database: raw
    schema: jaffle_shop #schema name
    tables:
      - name: customers
      - name: orders
        config:
          freshness:
            warn_after: 
              count: 240
              period: hour
            error_after:
              count: 480
              period: hour
          loaded_at_field: _etl_loaded_at
